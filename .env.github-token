#!/bin/bash
# Minimal GitHub Token Configuration for claude-flow-baseline
# Maps $_GITHUB_PAT to $GITHUB_TOKEN for cross-repository operations

if [ -n "$_GITHUB_PAT" ]; then
    export GITHUB_TOKEN="$_GITHUB_PAT"
    export GH_TOKEN="$_GITHUB_PAT"
fi
